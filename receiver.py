#!/usr/bin/env python3

import os
import time
import pickle
from filelist import *
from options import *
import message

def receiver(source):
    # ici -r sera utilisé ?
    liste = lister(source)
    return liste
